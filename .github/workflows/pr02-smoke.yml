name: PR-02 smoke

on:
  push:
    branches:
      - feat/authoritative-demand-evaluator-v1
  pull_request:
    branches:
      - main

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo smoke
